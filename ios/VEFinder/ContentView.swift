import SwiftUI

struct ContentView: View {
    var body: some View {
        WebView(url: URL(string: "https://6yytn6brg6-ctrl.github.io/ve-finder/?native=1")!)
            .ignoresSafeArea()
    }
}
